import { DashToSpacesPipe } from './dash-to-spaces.pipe';

describe('DashToSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new DashToSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
